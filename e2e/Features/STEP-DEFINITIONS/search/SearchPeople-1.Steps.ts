


/* defineSupportCode(function ({Given, When, Then}) {
  Given(/^I choose the People option on the page$/, function() {
    return true;
  });
  When(/^I set a `characterName` in the input field$/, function() {
    return true;
  });
  Then(/^I expect the related results include `Gender`, `BirthYear`, `EyeColor`, `SkinColor`$/, function() {
    return true;
  });
});
 */